apiVersion: v1
kind: Service
metadata:
  name: task-service
  namespace: devops
spec:
  selector:
    app: task-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
  type: NodePort
